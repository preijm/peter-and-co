# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.suo
*.user

# Local dev
node_modules/
.env
dist/
.claude/

# Internal reference files
_intern/
# Footage: HEIC originals are converted to .jpg by `npm run footage` and it's the
# JPEG that ships. Keep the originals in OneDrive, not in git.
footage/*.heic
footage/*.HEIC
footage/*.heif
# Footprint tool: local-only absolute paths (never commit)
tools/footprint/archive.local.json
